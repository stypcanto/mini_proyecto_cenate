# ========================================================================
# ⚙️ DOCKER COMPOSE - CENATE PLATFORM (SOLUCIÓN PARA MAC)
# Usa extra_hosts para permitir acceso a PostgreSQL en red externa
# ========================================================================

services:
  # ------------------------------------------------------------
  # 🧩 BACKEND - SPRING BOOT (Java 17 / Gradle Build)
  # ------------------------------------------------------------
  backend:
    container_name: cenate-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      # 🗄️ Base de datos
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL:-jdbc:postgresql://10.0.89.241:5432/maestro_cenate}
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME:-postgres}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD:-Essalud2025}
      SPRING_JPA_HIBERNATE_DDL_AUTO: none
      SPRING_JPA_SHOW_SQL: "true"

      # 🔐 JWT
      JWT_SECRET: ${JWT_SECRET:-404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
      JWT_EXPIRATION: ${JWT_EXPIRATION:-86400000}

      # 🌍 Zona horaria
      TZ: America/Lima
    ports:
      - "8080:8080"
    volumes:
      # 📸 Volumen para archivos subidos (EXTERNO - fuera del proyecto Docker)
      - /var/cenate-uploads:/app/uploads
    networks:
      - cenate-net
    # Permitir acceso a IPs de la red externa
    extra_hosts:
      - "db-server:10.0.89.241"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2.5G


  # ------------------------------------------------------------
  # 💻 FRONTEND - REACT + NGINX
  # ------------------------------------------------------------
  frontend:
    container_name: cenate-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    depends_on:
      - backend
    ports:
      - "80:80"
    environment:
      - TZ=America/Lima
    networks:
      - cenate-net
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M

# ========================================================================
# 🌐 RED INTERNA DOCKER
# ========================================================================
networks:
  cenate-net:
    driver: bridge
