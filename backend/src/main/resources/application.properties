# ============================================================
# CONFIGURACIÓN GENERAL
# ============================================================
spring.application.name=CenateApplication
server.port=8080
server.address=0.0.0.0

# ============================================================
# SPRING BOOT DEVTOOLS (🚀 MODO DESARROLLO)
# ============================================================
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false
spring.devtools.restart.additional-paths=src/main/java
spring.devtools.restart.exclude=static/**,public/**,templates/**

# ============================================================
# SPRING WEB MVC
# ============================================================
spring.web.resources.add-mappings=false
spring.mvc.throw-exception-if-no-handler-found=true

# ============================================================
# TOMCAT SERVLET THREADS - OPTIMIZADO PARA 100 USUARIOS
# ============================================================
# ✅ v1.37.3 - Thread pool aumentado para soportar 100 usuarios concurrentes
server.tomcat.threads.max=200
server.tomcat.threads.min-spare=20
server.tomcat.accept-count=100
server.tomcat.max-connections=200
server.tomcat.connection-timeout=60000
# Async request handling para no bloquear threads
server.tomcat.threads.naming-prefix=cenate-http-

# ============================================================
# CONEXIÓN A POSTGRESQL
# ============================================================
# IMPORTANTE: Configurar variables de entorno en produccion
# Ver: spec/008_plan_seguridad_auth.md para detalles
spring.datasource.url=${DB_URL:jdbc:postgresql://10.0.89.13:5432/maestro_cenate?characterEncoding=UTF-8&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Essalud2025}
spring.datasource.driver-class-name=org.postgresql.Driver

# 🆕 v1.13.8: Configuración de charset para PostgreSQL
spring.datasource.hikari.connection-init-sql=SET client_encoding='UTF8'

# ============================================================
# HIKARI - POOL DE CONEXIONES
# ============================================================
# ✅ v1.37.3 - AUMENTADO de 10 a 100 para soportar 100 usuarios concurrentes
spring.datasource.hikari.maximum-pool-size=100
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.keepalive-time=300000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.pool-name=CenateHikariCP
spring.datasource.hikari.auto-commit=true
# Monitoreo de pool en producción
logging.level.com.zaxxer.hikari=WARN
logging.level.org.postgresql=WARN

# ============================================================
# JPA / HIBERNATE - OPTIMIZADO PARA 100 USUARIOS
# ============================================================
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.open-in-view=false

# ✅ v1.37.3 - Optimizaciones Hibernate para 100 usuarios
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.fetch_size=50
spring.jpa.properties.hibernate.generate_statistics=false

# Query Cache (L2 Cache) - 🔧 DESHABILITADO: Usar Caffeine en futuras versiones
# Para v1.37.3, optimizamos con Batch Processing en su lugar
spring.jpa.properties.hibernate.cache.use_second_level_cache=false
spring.jpa.properties.hibernate.cache.use_query_cache=false
# spring.jpa.properties.hibernate.cache.region.factory_class=org.hibernate.cache.jcache.JCacheRegionFactory
# spring.jpa.properties.hibernate.javax.cache.provider=org.ehcache.jsr107.EhcacheManager

# Statement caching
spring.datasource.hikari.data-source-properties.preparedStatementCacheSize=250
spring.datasource.hikari.data-source-properties.preparedStatementCacheSqlLimit=2048
spring.datasource.hikari.data-source-properties.cachePreparedStatements=true

# ============================================================
# LOGGING - OPTIMIZADO PARA 100 USUARIOS (DESARROLLO)
# ============================================================
# ✅ v1.37.3 - Reducir logging para mejor rendimiento
logging.level.root=WARN
logging.level.com.styp.cenate=INFO
logging.level.org.springframework=WARN
logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=WARN
logging.level.org.springframework.context.annotation.ClassPathBeanDefinitionScanner=WARN
logging.level.org.springframework.web.servlet=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
# Async logging para no bloquear threads
logging.pattern.console=%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
server.error.include-stacktrace=never
server.error.include-message=always

# ============================================================
# JWT CONFIG
# ============================================================
# IMPORTANTE: JWT_SECRET debe tener minimo 32 caracteres
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
# ✅ v1.37.3 - Aumentado a 12h (43200000ms) para mejor UX bajo carga
jwt.expiration=43200000

# ============================================================
# CORS - PRODUCCIÓN Y DESARROLLO
# ============================================================
cors.allowed-origins=http://127.0.0.1:5500,http://127.0.0.2:5500,http://localhost:3000,http://127.0.0.1:3000,http://localhost,http://localhost:80,http://10.0.89.13,http://10.0.89.13:80,http://10.0.89.13:3000,http://10.0.89.239,http://10.0.89.239:80,http://10.0.89.239:3000
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
cors.allowed-headers=*
cors.allow-credentials=true
cors.max-age=3600

# ============================================================
# ACTUATOR
# ============================================================
#management.endpoints.web.exposure.include=health,info,mappings
management.endpoints.web.exposure.include=health,info,mappings,metrics,prometheus
#management.endpoint.health.show-details=when-authorized
management.endpoint.health.show-details=always
# Deshabilitar health check de mail (evita timeout en redes donde SMTP no es accesible)
management.health.mail.enabled=false
management.server.port=9090
# ============================================================
# SWAGGER / OPENAPI
# ============================================================
springdoc.api-docs.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
# -- Config adicional para IGU-SWAGGER
springdoc.swagger-ui.config-url=/api-docs/swagger-config
springdoc.swagger-ui.url=/api-docs


# ============================================================
# JACKSON (Fechas, Zona Horaria, y Charset)
# ============================================================
spring.jackson.time-zone=America/Lima
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.encoding.charset=UTF-8

# 🆕 v1.13.8: Asegurar UTF-8 en respuestas HTTP
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# ============================================================
# COMPRESIÓN HTTP Y CACHING - OPTIMIZADO PARA 100 USUARIOS
# ============================================================
# ✅ v1.37.3 - Compresión gzip para reducir tráfico de red
server.compression.enabled=true
server.compression.min-response-size=1024
server.compression.mime-types=text/html,text/xml,text/plain,text/css,application/javascript,application/json
# Browser caching para assets estáticos
spring.web.resources.cache.period=86400
spring.web.resources.cache.cachecontrol.max-age=86400
spring.web.resources.cache.cachecontrol.public=true

# ============================================================
# MULTIPART FILE UPLOAD - CONFIGURACIÓN DE ARCHIVOS
# ============================================================
# Tamaño máximo por archivo: 5MB
spring.servlet.multipart.max-file-size=5MB
# Tamaño máximo total de la petición: 10MB
spring.servlet.multipart.max-request-size=10MB
# Habilitar multipart
spring.servlet.multipart.enabled=true

# ============================================================
# CONFIGURACIÓN DE FOTOS DE PERFIL
# ============================================================
# Directorio donde se almacenan las fotos de perfil
app.upload.foto.dir=/app/uploads/fotos
# URL base para acceder a las fotos
app.upload.foto.url=/api/personal/foto

# ============================================================
# CONFIGURACIÓN DE ALMACENAMIENTO TELEECG
# ============================================================
# Ruta base para almacenamiento de imágenes ECG
# Producción: /opt/cenate/teleekgs
# Desarrollo: /tmp/cenate/teleekgs (creado automáticamente)
app.teleekgs.storage.base-path=${TELEEKGS_BASE_PATH:/tmp/cenate/teleekgs}

# ============================================================
# CONFIGURACIÓN DE EMAIL (SMTP Corporativo EsSalud)
# ============================================================
# Servidor SMTP principal (EsSalud Corporativo)
spring.mail.host=${MAIL_HOST:172.20.0.227}
spring.mail.port=${MAIL_PORT:25}
spring.mail.username=${MAIL_USERNAME:cenate.contacto@essalud.gob.pe}
spring.mail.password=${MAIL_PASSWORD:essaludc50}

# ⚠️ CONFIGURACIÓN CRÍTICA: Si el servidor corporativo no funciona, usar Gmail
# Habilitar fallback a Gmail para desarrollo (variable de entorno)
# MAIL_USE_GMAIL_FALLBACK=true
app.mail.use-gmail-fallback=${MAIL_USE_GMAIL_FALLBACK:false}
app.mail.gmail-username=${MAIL_GMAIL_USERNAME:cenateinformatica@gmail.com}
app.mail.gmail-password=${MAIL_GMAIL_PASSWORD:nolq uisr fwdw zdly}

# Configuración de autenticación para servidor corporativo
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
spring.mail.properties.mail.smtp.ssl.enable=${MAIL_SMTP_SSL:false}
spring.mail.properties.mail.smtp.starttls.required=${MAIL_SMTP_STARTTLS_REQUIRED:false}

# Timeouts (aumentados para evitar timeouts prematuros)
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000

# Debug SMTP (activar para diagnóstico)
spring.mail.properties.mail.debug=${MAIL_DEBUG:true}
logging.level.org.springframework.mail=DEBUG
logging.level.com.sun.mail=DEBUG
logging.level.jakarta.mail=DEBUG

# Nombre del remitente
app.mail.from-name=CENATE Sistema
app.mail.from-address=${MAIL_USERNAME:cenate.contacto@essalud.gob.pe}

# ============================================================
# URL DEL FRONTEND (para enlaces en emails)
# ============================================================
# Producción: export FRONTEND_URL=http://10.0.89.239
# Desarrollo: usa localhost:3000 por defecto
app.frontend.url=${FRONTEND_URL:http://localhost:3000}

# ============================================================
# CONFIGURACIÓN JITSI JaaS
# ============================================================
jitsi.jaas.app-id=vpaas-magic-cookie-c0d9ddbe35d14e2893148b58009721f6
jitsi.jaas.key-id=vpaas-magic-cookie-c0d9ddbe35d14e2893148b58009721f6/5f00e5-SAMPLE_APP
jitsi.jaas.domain=8x8.vc
jitsi.jaas.private-key-path=classpath:keys/jaas-private.pk


