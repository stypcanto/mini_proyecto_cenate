# ============================================
# CONFIGURACIÓN DE ENTORNO - EJEMPLO
# Copiar este archivo como .env y completar los valores reales
# NUNCA subir el archivo .env a Git
# ============================================

# Base de Datos PostgreSQL
DB_URL=jdbc:postgresql://localhost:5432/maestro_cenate
DB_USERNAME=postgres
DB_PASSWORD=TU_PASSWORD_AQUI

# JWT Configuration (generar un secret seguro de 64+ caracteres)
JWT_SECRET=GENERA_UN_SECRET_SEGURO_DE_64_CARACTERES_MINIMO

# Email (Gmail SMTP con App Password)
MAIL_USERNAME=tucorreo@gmail.com
MAIL_PASSWORD=xxxx-xxxx-xxxx-xxxx

# URL del Frontend (para enlaces en emails de recuperación)
FRONTEND_URL=http://localhost:3000

# Server Configuration
SERVER_PORT=8080
SERVER_ADDRESS=0.0.0.0

# CORS Allowed Origins (separados por coma)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
